
import { connect } from 'react-redux'
import { GrinPoolDetailsComponent } from '../../containers/GrinPoolDetails/GrinPoolDetails.js'

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  return {

  }
}

export const GrinPoolDetailsConnector = connect(mapStateToProps, mapDispatchToProps)(GrinPoolDetailsComponent)
